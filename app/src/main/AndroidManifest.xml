<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.colornote">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="q">
        <activity android:name=".ScreenLogin"></activity>
        <activity android:name=".View.GoogleCalendar" />
        <activity android:name=".View.WebView" />
        <activity android:name=".View.TrashCanView" />
        <activity android:name=".View.SearchView" />
        <activity android:name=".View.ArchivesView" />
        <activity android:name=".View.Setting" />
        <activity android:name=".View.CheckList" />
        <activity android:name=".View.SetPassword" />
        <activity android:name=".View.InputContent" />
        <activity android:name=".View.ShowNotes" />
        <activity android:name=".View.MainActivity" />
        <activity
            android:name=".View.LoginView"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.CalendarView" />

        <receiver
            android:name=".Service.AlarmReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".Service.AlarmNotification"
            android:enabled="true"
            android:exported="true" />
    </application>

</manifest>